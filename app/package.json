{
  "name": "experimental-coding-gallery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:dev": "node scripts/sync-experiments.mjs --mode=dev",
    "sync:build": "node scripts/sync-experiments.mjs --mode=build",
    "predev": "npm run sync:dev",
    "dev": "vite",
    "prebuild": "npm run sync:build",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
